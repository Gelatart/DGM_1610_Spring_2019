using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shield : MonoBehaviour //Make PowerUpBase when can?
{
    
    //ValueCap = 50f;
    // Start is called before the first frame update
    void OnEnable()
    {
       // Value = 15;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
